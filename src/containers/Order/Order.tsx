import CategoryList from "../../components/CategoryList/CategoryList";

const Order = () => {
  return (
    <>
      <CategoryList categoryName="order" onClick={() => {}} />
    </>
  );
};

export default Order;
